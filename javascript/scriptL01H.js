let comprimento, largura, altura, volume;

alert("Digite o valor do comprimento:");
comprimento = parseInt(prompt());

alert("Digite o valor da largura:");
largura = parseInt(prompt());

alert("Digite o valor da altura:");
altura = parseInt(prompt());

volume = comprimento * largura * altura;

alert("Volume é igual a " + volume);
